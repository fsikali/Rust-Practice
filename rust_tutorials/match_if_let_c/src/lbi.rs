/*
- We can include an else with an if let
- The block of code that goes with the else is the same as the block of code that would go
  with the _case in the match expression that is equivalent to the if let and else.
- Recall the Coin enum definition, where the Quarter variant also held a UsState value. 
- If we wanted to count all non-quarter coins we see while also announcing the state of the
  quarters, we could do that with a match expression e.g
 */ 

  
