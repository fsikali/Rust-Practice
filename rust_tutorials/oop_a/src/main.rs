/*
--- An AveragedCollection struct that maintains a list of integers and the average of the items in the
    collection
*/

pub struct AveragedCollection { 
    list: Vec<i32>, 
    average: f64,
}

